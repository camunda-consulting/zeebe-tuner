apiVersion: batch/v1
kind: Job
metadata:
  name: zbctl-deploy
spec:
  template:
    spec:
      containers:
      - name: zbctl-deploy
        image: camunda/zeebe
        command: ["zbctl", "--host", "camunda-zeebe-gateway", "--insecure", "deploy", "/models/process-8-service-tasks.bpmn"]
        volumeMounts:
        - name: models
          mountPath: /models/process-8-service-tasks.bpmn
          subPath: process-8-service-tasks.bpmn
          readOnly: true
      volumes:
      - name: models
        configMap:
          name: models
      restartPolicy: Never
