# path to the directory where testruns are stored
# absolute or relative to runner directory
TESTRUNS_DONE_DIR = testruns-done
# path to the directory where models are stored
# absolute or relative to runner/current/run
modelDir = ../../../models